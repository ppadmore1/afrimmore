project_id = "exxrhowfpflufhxdcavo"

# User-facing functions: JWT verification enabled (default)
# These functions also have internal auth validation for defense-in-depth

# scheduled-stock-alert: JWT disabled for cron job access (uses service role internally)
[functions.scheduled-stock-alert]
verify_jwt = false
